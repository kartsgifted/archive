# K예술영재 아카이빙 사이트

## 개요
- 교강사·운영진·발주기관이 접근코드로 들어와 워크숍·심화 멘토링·겨울 심화캠프의 영상·사진·학생 작품을 일정표 기준으로 찾아보는 **비공개 아카이브**다. 학생·학부모는 이용 대상이 아니다.
- 1차 개발 목표는 **2026-09-30**(10월 1주 오픈)이다. 시트에 입력하면 바로 동작하는 **사이트 틀**을 완성하며, 개발·테스트는 **음악 분야 시범 입력** 자료로 한다.
- 구성: 화면은 HTML·CSS·바닐라 JS(GitHub Pages), 관문은 Google Apps Script 웹앱, 데이터는 Google Sheets, 영상은 Vimeo, 사진은 Google Drive를 쓴다. **서버·DB·프레임워크를 새로 도입하지 않는다.**
- 저장소 구조: 열람 화면은 `site/`, 관문 코드는 `apps-script/`, 기준 문서는 `docs/`에 둔다. 앞의 두 폴더는 개발 착수 시 만든다.
- 저장소 루트의 `index.html` · `admin.html` · `conference.html` · `data.json` · `bg_*.jpg`는 **기존 워크숍 아카이브**다. 참고용으로 두며 합의 없이 고치지 않는다.
- 홍보 사이트와는 별도이며, 두 사이트는 서로 링크하지 않는다.

## 보안 (반드시 지킬 것)
1. 화면 코드에는 데이터·비밀값·권한 판단을 두지 않는다. 화면 코드와 관문 주소는 공개된다고 가정한다.
2. 인증·토큰·공개여부·자료 허용 여부는 관문이 **요청마다** 검증한다. 공개여부가 `공개`인 자료만 내보낸다.
3. 서명 키는 Script Properties에만 둔다. 토큰은 요청 본문으로만 보낸다(URL 금지).
4. 토큰은 `sessionStorage`에 둔다. `localStorage`에는 기기ID만 둔다.
5. 외부 입력은 `textContent`로 출력한다. `innerHTML`에 넣지 않는다. 기존 화면 코드를 재활용할 때는 `innerHTML` 삽입부를 먼저 `textContent`로 바꾼 뒤 옮긴다.
6. 시트에 쓰는 값은 수식 주입을 막는다(`=` `+` `-` `@`로 시작하면 앞에 `'`).
7. 실제 접근코드, 서명 키, 시트 ID, 드라이브 폴더 ID, 관문 배포 주소, 학생 실명은 어떤 파일에도 쓰지 않는다. 이런 값은 `.gitignore`에 등록된 `docs/private/`에만 둔다.
8. GitHub Pages로 배포하는 것은 `site/` 폴더뿐이다. `docs/` · `apps-script/` · `_handoff/`는 배포하지 않는다.
9. **이 저장소가 기존 워크숍 아카이브 저장소다.** 루트의 기존 파일과 전체 커밋 이력에 학생 실명·평문 관리자 암호·무인증 관문 주소가 들어 있다. 새 코드로 옮기지 않는다.
10. 외부 라이브러리를 추가하기 전에 먼저 묻는다.

## 작업 방식
- 작업 브랜치는 `archive/parkjiheon`이다. `main` 병합은 담당자가 직접 한다.
- 작업을 시작하기 전에 `docs/wbs.md`에서 해당 WBS ID를 확인하고, 커밋 메시지 앞에 붙인다(예: `[2.5] 토큰 발급·연장`).
- 설계와 다르게 구현해야 하면 먼저 묻는다. 합의되면 `docs/decisions.md`도 함께 고친다.
- 문서에 없는 사실은 지어내지 말고 "확인 필요"로 표시한다.
- 파일을 지우거나 덮어쓰기 전, 그리고 커밋하기 전에 먼저 묻는다. 커밋 전에는 비밀값·실명이 포함됐는지 확인한다.

## 참고 문서 (해당 작업 전에 읽을 것)
- `docs/decisions.md` — 확정 결정사항과 이유, 화면 규칙, 남은 결정 사항
- `docs/wbs.md` — 1차 개발 일정과 진행 상태
- `docs/architecture.md` — 시스템 구성과 요청 흐름
- `docs/screens.md` — 6단계 화면 흐름, 화면별 상세, 상시 검색 (화면 작업 시)
- `docs/api.md` — 관문 요청·응답·오류 코드, 토큰·실패 제한 (관문 작업 시)
- `docs/sheet-schema.md` — 시트 탭·열·선택 목록·코드 형식 (시트 작업 시)
