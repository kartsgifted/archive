# 결정사항

> 근거: 「K예술영재 프로젝트 아카이빙 사이트 구축 방안」(2026-09-14) + 이후 협의
> 최종 정리 2026-09-16
> 구축방안 PDF는 보고용 문서이며, 이후 협의로 바뀐 내용은 이 문서를 기준으로 한다. 바뀐 항목은 「10. 구축방안 PDF에서 바뀐 것」에 모아 두었다.

---

## 1. 목표와 범위

| 항목 | 결정 | 이유 |
|---|---|---|
| 1차 개발 완료 | **2026-09-30** → 10월 1주 오픈 | 10월 권역별 심화 멘토링 개시 전에 교강사가 워크숍 기록을 참조할 수 있어야 함 |
| 1차 개발의 성격 | 실제 자료가 생기면 시트에 입력하는 즉시 동작하는 **사이트 틀** 완성 | 자료 확보 시점과 개발 일정을 분리해야 일정이 지켜짐 |
| 개발·테스트 자료 | **음악 분야 시범 입력** | 4개 분야를 모두 입력한 뒤 서식을 바꾸면 재작업 부담이 큼. 한 분야를 끝까지 입력해 보고 서식을 보완한 뒤 확대함 |
| 이용 대상 | 교강사 · 운영진 · 발주기관 | 워크숍 자료에 참가 학생 얼굴·성명이 그대로 담겨 일반 공개가 불가능함 |
| 이용 대상 제외 | **학생 · 학부모** | 위와 같음 |
| 대상 프로그램 | 분야별 워크숍 · 권역별 심화 멘토링 · 겨울 심화캠프 | 구축방안 수록 범위 |
| 자료 유형 | 영상 · 사진 · 학생 작품 결과물 | 구축방안 수록 범위 |
| 홍보 사이트와의 관계 | 별도 구축, 상호 링크 없음. 주소는 대상자에게 개별 안내 | 검색·링크를 타고 외부인이 유입되는 경로를 없앰 |
| 범위에서 제외 | 영상 데이터 확보 시점, 소유 계정 구분 | 틀을 먼저 정하는 단계이므로 이번에는 다루지 않음 |

---

## 2. 사이트 구조

| 항목 | 결정 | 이유 |
|---|---|---|
| 탐색 방식 | 목록 나열이 아니라 **실제 일정표를 화면에 옮기고, 시간대를 고르면 그때 생산된 자료가 열리는 구조** | 이용자는 "며칠 몇 시에 어떤 수업이었는지"를 이미 알고 있는 경우가 대부분 |
| 메뉴 방식 | 메뉴형이 아닌 **단계형 화면 + 상시 검색** | 단계마다 범위가 좁혀져 자료를 찾는 경로가 하나로 유지됨 |
| 흐름 | 로그인 → 분야 → 프로그램 → 일정표 → 목록 → 열람 (6단계) | 위와 같음 |
| 자료 목록을 거치는 이유 | 한 시간대에 자료가 여러 건인 경우가 일반적(카메라 복수 운용, 녹화 분할, 사진 다수) | 시간대에서 바로 영상으로 가면 나머지 자료가 묻힘 |
| 공통 상단바 | 로고 · 경로 표시 · 통합검색 · 로그아웃 | 어느 단계에서도 검색과 이탈이 가능해야 함 |
| 프로그램 선택 | 워크숍 · 심화 멘토링 · 겨울 심화캠프 (3개) | 학생 작품은 시간대에 귀속되지 않아 별도 카드(4번째 프로그램)로 넣었었으나, 검색 → 인물 상세를 통해서만 접근하는 쪽으로 되돌림(2026-09-17) |
| 귀속 기준 | 워크숍 = 일자 × 시간대 / 멘토링 = 분야 × 권역 × 회차 / 캠프 = 일자 | 프로그램마다 일정이 짜이는 단위가 다름 |
| 학생 작품 | 학생 · 분야 단위로 관리하되 별도 화면 진입점은 없음(검색으로만 노출) | 위와 같음 |
| 라우팅 | 해시 라우팅 (`#/music/workshop/세션ID`) | 새로고침 없이 이동하고, 링크 공유가 가능함 |
| 데이터 로딩 | 인증 직후 공개 자료를 한 번에 받고, 이후 화면 이동·검색은 브라우저에서 처리 | 요청 수를 줄여 Apps Script 실행 한도를 아낌 |
| auth와 data 분리 | 로그인(auth) 응답에 자료를 담지 않고, 화면은 auth 응답만 받으면 바로 진입한 뒤 data를 뒤에서 받음 | 자료를 로그인 응답에 함께 담았을 때 로그인 대기가 너무 길어 분리함. 합치면 로그인도 data 전체를 기다리게 됨 |
| 기존 자산 | 기존 워크숍 영상 아카이브 화면을 **재활용**하되, 데이터와 로그인 방식은 가져오지 않음 | 일정표 격자·한글 입력 보정 등은 그대로 쓸 값어치가 있으나, 기존 접근코드 검증은 브라우저에서만 이뤄져 새 설계와 맞지 않음 |

---

## 3. 화면 규칙

| 항목 | 규칙 |
|---|---|
| 흐름 | 로그인 → 분야 → 프로그램 → 일정표 → 목록 → 열람 |
| 프로그램 | 워크숍 · 멘토링 · 캠프 (학생 작품은 검색으로만 접근) |
| 첫 화면 | 분야 코드 → 해당 분야 / STAFF → 분야 선택 |
| 일정표 | 워크숍 격자 · 멘토링 격자 · 캠프 카드, 빈 칸은 "자료 없음" |
| 검색 | 분야 · 활동명 · 수업명 · 교강사 · 학생 · 장소 |
| 로그인 유지 | 새로고침 유지 / 탭 닫으면 해제 |
| 금지 | 워터마크 표시, 영상 내려받기, 검색엔진 노출 |

**보충**

| 항목 | 결정 | 이유 |
|---|---|---|
| 분야 이동 | 코드에 따라 첫 화면만 다르고, 다른 분야로의 이동은 제한하지 않음 | 교강사가 타 분야 교수법을 참고하는 것이 이 사이트의 주요 이용 목적 중 하나임 |
| 일정표 표시 | 자료 보유 칸을 강조해 미보유 칸과 구분 | 빈 칸을 눌러보는 헛걸음을 줄임 |
| 자료 목록 | 영상 · 사진 · 작품 탭으로 구분 | 유형별로 보는 방식이 달라 섞이면 찾기 어려움 |
| 열람 | 영상은 화면 안 재생, 사진·작품은 확대 보기, 참여자 칩 → 인물 상세 이동 | 영상이 사이트 밖에서 열리면 도메인 제한이 무력해짐 |
| 검색 결과 | 사람 · 수업 · 작품으로 구분. 인물 상세에 참여 이력과 함께 참여한 인물 | 한 번의 입력으로 전 항목을 조회하되 결과 성격이 섞이지 않게 함 |
| 인물 상세 | 그 학생이 참여한 클래스 영상 + 만든 작품을 함께 표시 | 2026-09-17 결정 |
| 동명이인 검색 | 학생ID(5절)로 구분되는 동명이인이 있으면 목록으로 보여주고 선택하게 함 | 성명만으로는 검색 결과가 서로 다른 학생을 합칠 수 있음 |
| 성명 검색 | 접근코드 인증자에게만 제공. 홍보 사이트에는 어떤 형태로도 제공하지 않음 | 학생 성명이 외부에 노출되면 안 됨 |
| 진입 화면 안내 | 자료의 외부 유출·재배포 금지를 명시 | 기술적 차단이 닿지 않는 부분을 안내로 보완 |

---

## 4. 시스템 구성

| 영역 | 결정 | 이유 |
|---|---|---|
| 열람 화면 | HTML · CSS · 바닐라 JS, GitHub Pages (HTTPS 강제) | 무료이고 주소가 고정됨 |
| 배포 방식 | Pages "브랜치에서 배포"(`main` 루트)를 그대로 두고, 루트 `_config.yml`의 `exclude`로 `docs/`·`apps-script/`·`CLAUDE.md`를 게시에서 뺌 (2026-09-18) | 기존 워크숍 아카이브(루트)를 살린 채 `site/`를 같은 배포에 얹기 위함. Pages 소스를 Actions로 바꾸는 방식은 전환 중 기존 사이트가 1~2분 내려가므로 채택하지 않음. 주소는 기존 `/archive/`, 새 화면은 `/archive/site/` |
| 관문 | **Apps Script 웹앱** (JSON만 응답) | 서버가 없고, 계정 이관만으로 인수인계가 끝남. FastAPI·Spring은 서버 비용과 인수인계 부담으로 미채택 |
| 데이터 | **Google Sheets** (공유 안 함) | 데이터 규모가 작고 비개발자가 직접 관리함. Cloud SQL 등은 상시 비용·인수인계 부담으로 미채택 |
| 영상 | **Vimeo** (1080p 압축본, 비공개, 도메인 제한, 내려받기 차단) | 간편함과 인수인계를 우선함. Bunny Stream은 대안(더 저렴, 토큰 인증 가능) |
| 사진 | Google Drive (공유 안 함). 대표 사진은 관문을 거쳐 축소본으로 전달 | 사진 주소가 유출돼도 열리지 않게 함 |
| 대표 사진 수량 | 한 수업당 5~8장만 화면에 직접 표시, 전체는 폴더로 연결 | 사진 전량 등록 시 워크숍 1회차당 수백 건이 발생해 입력이 불가능함. 대표 사진 선별은 홍보용으로도 필요한 작업이라 결과물을 공유해 중복 작업을 막음 |
| 원본 영상 | 외장 저장장치 2개에 이중 보관, 사이트와 연결하지 않음 | 8TB 규모를 온라인에 둘 이유가 없음 |
| 교체 가능성 | 관문 응답 형식 `{ok, error, data}` 고정, 영상 서비스는 설정값으로 선택 | 나중에 관문·영상 서비스만 갈아끼울 수 있게 함 |
| 백엔드 개발 도구 | **clasp**(Google 공식 CLI) 도입 | `apps-script/`를 VSCode에서 작성해 Apps Script 프로젝트로 동기화(push)·배포하기 위함. 웹 편집기에 수동으로 옮기는 방식은 파일이 늘수록 누락·구버전 반영 위험이 커짐 |
| 웹앱 실행 설정 위치 | `appsscript.json`의 `webapp` 필드에 명시(`executeAs: USER_DEPLOYING`, `access: ANYONE_ANONYMOUS`) | 이 설정이 매니페스트에 없으면 `clasp deploy`로 기존 배포에 새 버전을 올릴 때마다 웹앱 접근 설정이 풀려 관문이 404로 막힘(2026-09-17 실제로 겪음). 코드로 고정해두면 재발하지 않음 |

자세한 구성과 요청 흐름은 `docs/architecture.md` 참조.

---

## 5. 시트 구성 (공통 1 + 프로그램별 3)

| 시트 | 탭 | 비고 |
|---|---|---|
| 공통 | 읽어보기 · 학생 · 코드 · 설정 · 로그 | 관문 스크립트가 붙는 시트. 메뉴도 여기에만 뜸 |
| 워크숍 | 읽어보기 · 일정 · 자료 · 출석부 · 참여자 | 세 프로그램 시트는 탭·열 구성이 같음 |
| 심화 멘토링 | 위와 같음 | |
| 겨울 심화캠프 | 위와 같음 | |

| 탭 | 입력 | 비고 |
|---|---|---|
| 읽어보기 | 안내 | 입력 규칙과 자료 유형 설명 |
| 일정 | 직접 | 세션ID 자동 생성. 프로그램 열 없음(시트가 프로그램을 정함) |
| 자료 | 직접 | **공개여부** 열: 검수대기(기본값) / 공개 / 비공개 |
| 출석부 | 직접 | 가로 수업 × 세로 성명. 동명이인은 학생ID도 기재 |
| 학생 | 직접 | 학생ID·성명 명부. 동명이인이 실제로 발견된 학생만 등록 |
| 참여자 | 자동 | 같은 시트의 출석부에서 변환 |
| 코드 | 직접 | **버전** 열 있음 (올리면 해당 코드의 모든 접속이 즉시 해제됨) |
| 설정 | 직접 | 사업연도, 영상 서비스, 안내 문구 등 |
| 로그 | 자동 | 일시 · 결과 · 코드 · 이름 · 기기ID · 요청 · 대상 자료 |

| 항목 | 결정 | 이유 |
|---|---|---|
| 프로그램별 시트 분리 | 한 시트(9개 탭)를 **공통 시트 + 프로그램 시트 3개**로 나눔. 학생·코드·설정·로그는 공통, 일정·자료·출석부·참여자는 프로그램별 (2026-09-18) | 한 탭에 모든 프로그램·회차가 섞이면 입력·검수가 어렵고, 특히 출석부 변환이 「참여자」 전체를 다시 쓰는 방식이라 한 프로그램 출석부만 반영하면 다른 프로그램 참여자가 지워짐. 학생ID·접근코드는 프로그램과 무관하게 하나여야 해서 공통에 남김. 실제 자료가 0건인 음악 시범 입력(4.4) 전에 나눠 옮길 데이터가 없음 |
| 프로그램 구분 방식 | 행에 프로그램 열을 두지 않고 **시트로 구분**. 관문이 읽은 시트에 따라 응답의 `program`을 채움 | 열을 두면 시트와 다른 값이 적히는 실수가 생김. 응답 모양이 그대로라 화면 코드를 고치지 않음 |
| 메뉴 위치 | **공통 시트에만** 두고 프로그램을 골라 실행 | 시트 메뉴는 스크립트가 붙은 시트에만 뜸. 프로그램 시트마다 붙이면 Apps Script 프로젝트가 4개가 되어 배포·인수인계 부담이 커짐. 불편이 크면 재검토 |
| 시트 ID 보관 | 프로그램 시트 ID는 Script Properties에만 둠 | 보안 원칙(시트 ID를 파일에 쓰지 않음) 유지 |
| 세션ID 형식 | `{사업연도 2자리}-{프로그램코드}-{분야코드}-…` (예: `26-ws-music-0807-1`). 사업연도는 「설정」 탭 값 (2026-09-18) | 연도가 없으면 다음 해 같은 날짜·권역·회차에서 ID가 겹침. 겨울캠프는 이듬해 1월이라 달력 연도가 아니라 사업연도를 씀. 프로그램코드로 ID만 보고도 소속 시트를 알 수 있음 |
| 운영 방식 | 행 추가 → [점검] → 검수 후 공개여부 "공개" (버튼 없이 즉시 반영, `data` 요청이 시트를 캐시 없이 바로 읽음) | 담당자가 행만 추가하면 화면이 갱신되게 함 |
| 세션ID 자동 생성 | 분야·일자·시간대를 넣으면 식별자를 자동 부여 | 수기 입력 시 오타 한 글자로 영상이 연결되지 않음 |
| 출석부 자동 변환 | 기존 출석부 형태 그대로 입력하면 참여자 목록으로 변환 | 한 줄씩 입력하면 한 분야만으로도 수백 행이 됨 |
| 영상 자동 매칭 | 촬영 파일명 규칙 적용 (`20261015_1330_음악_A.mp4`) | 향후 촬영분부터 적용. 기촬영분은 업로드 일시와 재생 시간을 대조해 초벌 연결 후 검수 |
| 입력 오류 방지 | 고정값 항목에 선택 목록(드롭다운) 적용 | 표기가 흔들리면 화면에서 묶이지 않음 |
| 보호 | 자동 생성 열과 참여자·로그 탭 잠금 | 자동 계산 값을 사람이 덮어쓰지 못하게 함 |
| 입력 부담 경감이 필요한 이유 | 반복 작업이 **7개월간** 지속됨 | — |
| 동명이인 구분 | 「학생」 탭에 학생ID(수기 순번, `ST-0001`)를 부여해 「참여자」·「자료」 탭에서 성명 대신 대조 | 성명만으로는 동일 인물 여부를 구분할 수 없어, 검색·인물 상세가 서로 다른 학생의 이력을 하나로 합칠 위험이 있음. 발견 빈도가 낮아 자동 생성 없이 수기로 운영 |
| 학생ID 형식 | 의미 없는 순번만 사용. 생년월일 등 개인정보를 형식에 담지 않음 | 생년월일은 성명보다 민감한 개인정보라 식별자 형태로 노출·유통되는 걸 피함. 구분이 목적이면 순번으로 충분함 |

자세한 열 구성은 `docs/sheet-schema.md` 참조.

---

## 6. 보안

### 6-1. 접근 통제 (1단계 적용, 워터마크 제외)

| 항목 | 결정 | 이유 |
|---|---|---|
| 구글 로그인 | **사용 안 함** | 계정마다 권한을 부여하는 운영 부담이 큼 |
| 코드 형식 | **`KA-MUSIC-7Q4F`** — 앞부분은 배포 단위, 뒤 4자리 무작위 | 유출 시 어느 경로인지 좁힐 수 있고, 해당 분야만 교체 가능함 |
| 코드 수 | 5개 (운영진 1 + 분야별 4: 음악·무용·전통예술·미술) | 배포·회수 단위를 구분하기 위함 |
| 열람 범위 | **모든 코드가 전체 자료 열람** | 교강사가 타 분야 교수법을 참고하는 것이 주요 이용 목적임 |
| 코드 사용기한 | 운영진 2027-03-31 / 분야별 교강사 2027-02-28 | 사업 기간에 맞춤 |
| 이름 입력 | 코드와 함께 입력, 로그에 기록 | 사후 추적용 |
| 실패 제한 | 기기별 5회 실패 시 10분 잠금 + 코드 앞부분별 누적 제한 + 급증 시 운영진 메일 | 자동 대입 시도를 막음 |
| 인증 유지 | 토큰을 **sessionStorage**에 보관. 새로고침·외부 사이트 복귀 시 유지, 탭·브라우저 종료 또는 새 탭에서는 재입력 | 공용 PC에서 탭만 닫아도 세션이 끊기게 함 |
| 장기 토큰 | 7일 장기 토큰은 **사용 안 함** | 위와 같음 |
| 토큰 유효시간 | 서버 기준 2시간, 사용 중 자동 연장 | 열람 중 끊기지 않으면서 방치된 세션은 만료됨 |
| 기기ID | localStorage의 무작위 값 (인증 수단 아님, 실패 제한·로그 구분용) | 인증에 쓰면 조작 가능함 |
| 워터마크 | **표시하지 않음** | 1단계 범위에서 제외 |

### 6-2. 실제 보호 장치

접근코드는 이용 편의를 위한 장치이며, 실제 보호는 아래 세 가지로 이중 구성한다.

1. **시트 비공개** — 관문을 거치지 않으면 자료에 접근할 수 없음
2. **영상 도메인 제한** — 본 사이트 외에서는 재생되지 않음
3. **검색엔진 색인 차단** — 사이트 주소가 검색 결과에 노출되지 않음

### 6-3. 원칙과 대응

| 항목 | 결정 | 이유 |
|---|---|---|
| 화면 코드 | 데이터·비밀값·권한 판단을 두지 않음 | 화면 코드는 공개·조작되어도 본인 브라우저에만 영향. 관문 주소는 노출을 전제로 함 |
| 검증 위치 | 인증·토큰·공개여부·자료 허용 여부를 **모두 관문에서 매 요청 검증** | 위와 같음 |
| 서명 키 | Script Properties에만 보관. 토큰은 HMAC 서명, 요청 본문으로만 전달 | 주소·코드에서 키가 드러나지 않게 함 |
| 입력·출력 | 로그 입력의 수식 주입 방지, 화면 출력은 `textContent` | 시트 수식 실행과 저장형 XSS를 막음 |
| 클릭재킹 | 관문은 HTML을 내보내지 않음. 화면은 삽입 감지 차단 스크립트 적용 | GitHub Pages는 응답 헤더를 설정할 수 없음 |
| 그 외 | CSP 메타로 연결 대상 제한, noindex · robots.txt | — |
| 저장소 | GitHub 2단계 인증 · 브랜치 보호 · 외부 라이브러리 최소화 | — |

### 6-4. 수용하는 잔여 위험

화면 촬영·녹화, 코드 전달, 복제 사이트의 관문 호출(영상은 도메인 제한으로 재생 불가), Apps Script 실행 한도 소진, 공개 저장소를 통한 주소 노출.

---

## 7. 비용

| 항목 | 결정 |
|---|---|
| 서버 비용 | **없음** (GitHub Pages · Apps Script · Google Sheets) |
| 발생 비용 | **영상 보관소(Vimeo) 구독료만 발생** |
| 인수인계 | 계정 이관만으로 완료 |

**Vimeo 참고 (2026년 요금제 변경):** 비공개·임베드 영상에 별도 용량 한도가 있고(Creator 50GB, Core 300GB, Professional 7TB — Creator 외에는 제3자 자료 기준), 도메인 제한은 Core 이상에서 제공된다. 8TB 원본을 그대로 올리면 Professional 한도를 넘으므로 압축본만 올린다. **결제 전 공식 페이지 확인 필요.**

---

## 8. 이후 일정 (1차 개발 범위 밖)

인수인계 시 참고. 근거는 구축방안 PDF 「추진 일정」이다.

| 시기 | 내용 | 산출 |
|---|---|---|
| 2026-10 ~ 12 | 멘토링 자료 순차 등록 | 월 단위 갱신 |
| 2027-01 | 겨울캠프 및 성과공연 자료 등록 | 갱신 |
| 2027-02 | 연간 자료 정리 및 **계정 이관** | 최종본 |

1차 개발(2026-09-15 ~ 09-30) 일정은 `docs/wbs.md` 참조.

---

## 9. 남은 결정 사항

| 항목 | 내용 | 상태 |
|---|---|---|
| 전체 사진 폴더 접근 | 구글 로그인을 쓰지 않으므로 폴더 링크를 어떻게 열람시킬지 (링크 공개 허용 / 요청 시 개별 제공 / 대표 사진만 제공). 화면은 임시로 자료 목록에 "사진묶음" 항목을 보여주되 클릭은 막아둠(2026-09-18, `site/js/view-materials.js`) | 미정 |
| Vimeo 요금제 | 압축본 용량에 따라 Core 또는 Professional | 미정 |
| 구독료 부담 주체 | 사업 종료 후 Vimeo 구독 유지 주체 | 미정 |
| 저장소 공개 여부 | **Public으로 확인됨**(2026-09-18). 작업 브랜치도 외부에서 열람 가능하므로 `docs/`·`apps-script/`의 내용은 이미 공개 상태다. 실제 비밀값은 `docs/private/`(gitignore)에만 두는 현재 방식을 유지한다 | 확인됨 |
| 기존 회의록 페이지의 관문 주소 | 루트 `conference.html`에 인증 없이 시트 전체를 반환하는 Apps Script 배포 주소가 들어 있음. 배포를 보관 처리할지 결정 필요 | **확인 필요** |
| 커밋 이력의 평문 암호 | 삭제된 `README.md`와 구버전 `admin.html`에 관리자 암호가 평문으로 남아 있음. 같은 암호를 다른 곳에서 쓰고 있다면 교체 필요 | **확인 필요** |
| 기존 루트 파일 5종 처리 | 개발 착수 시 코드 분석 후 결정 (지금은 그대로 둠) | 보류 |
| 시스템 구성도 정리 | WBS와 같은 기준(개발 중심)으로 미결 사항을 정리할지 | 미정 |
| 다년 운영 여부 | 다음 사업연도에도 이 아카이브를 이어 쓰는지. 세션ID에는 일단 사업연도를 넣어 둠(5절) | **확인 필요** |
| 학생 작품의 소속 시트 | 작품이 만들어진 프로그램 시트에 적는 것이 원칙. 어느 프로그램에도 속하지 않는 작품이 있는지 | **확인 필요** |
| 관리자 페이지 | 시트를 직접 열지 않고 행을 추가·삭제하는 화면. 공개 웹 관리자 페이지 / 시트 안 입력 양식(사이드바) / 만들지 않음 중 선택. 공개 웹으로 만들면 공개된 관문에 쓰기 기능이 생겨 관리자 인증·권한·변경 기록을 새로 만들어야 함 | 미정 |
| 같은 분야 동시간대 수업 | 한 분야에서 반을 나눠 같은 시간에 수업하는 경우가 있는지. 세션ID는 순번으로 구분되지만, 워크숍 일정표는 한 칸에 수업 하나만 그려 나중 것이 앞의 것을 덮고(`site/js/view-schedule.js` 격자), [영상 매칭]은 파일명(일자·시각·분야)만으로 어느 반인지 알 수 없어 첫 수업에 붙임. 있다면 일정표 칸 안 여러 수업 표시 + 파일명에 반 구분 추가가 필요. 없다면 [점검]에 겹침 경고만 추가. 시범 데이터 46행에는 겹침 없음 | **확인 필요** |
| 응답 속도 (목표 5초) | 로그인·새로고침 모두 **5초 이내**가 목표(2026-09-18 사용자 요구). 측정 결과(09-18): 스크립트 실행 자체는 1~2초(길면 8.7초, 시트 분리 후 data가 2~4초 늘어남)이지만, **시트를 건드리지 않는 빈 요청도 10번 중 4번이 30~60초** 걸림 — 실행 기록에 잡히지 않는 Google 입구 대기. 예전 버전(@3)도 같은 현상. 계획: ① 코드·설정·data 응답 캐시 + 시트 편집 시 캐시 비우는 트리거 ② 접속 기록을 모아 1분마다 기록 ③ 새로고침도 바로 진입 후 data를 뒤에서 받기 ④ 3초 무응답 시 예비 요청. 이것으로도 입구 대기가 겹치는 경우의 5초 **보장**은 불가 — 보장이 필요하면 관문 서비스 교체(서버 미도입 원칙과 충돌) 논의 필요 | 진행 예정 (09-21) |
| 관리자 인원·역할 | 관리자 수, 입력 담당과 공개 승인 담당이 같은지. 탭·열 보호 설정이 이것에 따라 달라짐 | **확인 필요** |

---

## 10. 구축방안 PDF에서 바뀐 것

PDF는 2026-09-14 보고용 문서다. 아래 항목은 이후 협의로 바뀌었으며, **이 문서의 내용이 기준**이다.

| 항목 | PDF | 현재 기준 |
|---|---|---|
| 시트 탭 수 | 7개 | **9개** (로그 탭, 「학생」 탭 추가) → 이후 **시트 4개**(공통 5탭 + 프로그램별 5탭 × 3)로 분리 (2026-09-18, 5절) |
| 세션ID 형식 | 없음 | `{사업연도}-{프로그램}-{분야}-…` (5절) |
| 접근코드 형식 | `KA-<구분>-<연도 2자리>` | **`KA-MUSIC-7Q4F`** (뒤 4자리 무작위) |
| 비용 문구 | "유지 비용이 발생하지 않으며" | **"서버 비용 없음, 영상 보관소 구독료만 발생"** |
| 프로그램 선택 | 3종 | **3종 그대로.** 중간에 학생 작품을 4번째 카드로 추가했다가(9/16), 검색 → 인물 상세로만 접근하는 쪽으로 되돌림(9/17) |
| 코드 탭 열 | 코드·이름·배포 대상·첫 화면·사용기한·상태·비고 | **버전 열 추가** |
| 전체 사진 폴더 | "전체는 폴더로 연결" | 구글 로그인을 쓰지 않기로 하여 **열람 방법 미정** (9절) |
| M1 일자 | — | **2026-09-18** (인수인계 문서의 9/16은 폐기) |
| 개발·테스트 자료 | "음악 분야 시범 입력 및 서식 보완" | 동일. 인수인계 문서의 "샘플 데이터"는 **음악 분야 시범 입력**을 뜻함 |
| 학생 식별 | 없음 | 동명이인 구분을 위해 **학생ID** 도입(「학생」 탭 신설, 「출석부」·「자료」·「참여자」 탭에 학생ID 열 추가, 5절) |
